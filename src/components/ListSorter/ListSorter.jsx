export default function ListSorter() {
  return (
    <div>
      <form className="sort-control">
        <select>
          <option>New</option>
        </select>
        <select>
          <option>Asc</option>
        </select>
      </form>
    </div>
  );
}
